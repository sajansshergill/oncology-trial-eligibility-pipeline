name: oncology_trial_eligibility
version: "1.0.0"
config-version: 2

profile: oncology_trial_eligibility

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

clean-targets:
  - target
  - dbt_packages

models:
  oncology_trial_eligibility:
    bronze:
      +materialized: view
      +schema: bronze
    silver:
      +materialized: table
      +schema: silver
    gold:
      +materialized: table
      +schema: gold
